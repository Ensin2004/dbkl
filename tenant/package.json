{
  "name": "dbkl",
  "version": "1.0.0",
  "scripts": {
    "frontend": "npm start --prefix tenant-frontend",
    "backend": "npm start --prefix tenant-backend",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""
  },

  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
